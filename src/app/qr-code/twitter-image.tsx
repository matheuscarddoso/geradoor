/* O card do X usa a mesma imagem do Open Graph. Reexportar, e não copiar: um
   texto que mudasse só num dos dois seria um erro invisível. */
export { default, alt, size, contentType } from "./opengraph-image";
